# Signal-Events

Domain signal/event system.

## Purpose

Provides a typed domain signal emitter for broadcasting and subscribing to domain events. Supports deadline signals, status changes, constraint violations, review requirements, and voice/listening session events.

## Features

- Emit typed domain signals
- Subscribe to specific signal types
- Unsubscribe handlers
- Full signal metadata including priority, source kernel, and entity references
