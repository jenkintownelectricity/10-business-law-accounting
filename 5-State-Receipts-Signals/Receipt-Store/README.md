# Receipt-Store

Domain receipt storage.

## Purpose

Stores all domain operation receipts, providing a complete audit trail of every action taken across the sovereign domain. Supports querying by entity, kernel, action, and time range.

## Features

- Emit and store domain receipts for all operations
- Query receipts by entity ID
- Query receipts by source kernel
- Query receipts by action type
- Query receipts by time range
